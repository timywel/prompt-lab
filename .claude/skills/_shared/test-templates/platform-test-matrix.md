# 平台特定测试矩阵

> 来源: expanded-macos-voice-input-prd.md / ios-accessible-navigation-prd.md
> 用途: 列出各平台特性测试项，包含测试类型、覆盖场景、预期结果

---

## 1. macOS 平台特性测试

### 1.1 CGEventTap 全局事件监听

| 测试项 | 测试类型 | 覆盖场景 | 预期结果 |
|--------|----------|----------|----------|
| Fn 键按下检测 | 单元 + 手动 | 在任意应用中按下 Fn 键 | 浮窗出现 < 100ms |
| Fn 键释放检测 | 单元 + 手动 | 在任意应用中释放 Fn 键 | 识别流程触发 |
| Fn 键与其他修饰键组合 | 手动 | Fn+Shift、Fn+Control、Fn+Option | 正确识别，不误触发 |
| Fn 键被系统占用 | 手动 | 模拟 F1-F12 映射为功能键 | 检测失败并提示用户配置替代键 |
| 快速连续按放 (<200ms) | 单元 | 按下后立即释放 | 忽略操作，不触发录音 |
| 应用切换时事件不丢失 | 手动 | 录音过程中切换应用 | 事件持续监听，不中断录音 |
| Accessibility 权限拒绝 | 手动 | 首次启动拒绝权限 | 弹出系统偏好设置引导 |
| Accessibility 权限撤销后恢复 | 手动 | 运行中撤销权限 | 检测到权限丢失，提示用户重新授权 |

### 1.2 CJK 输入法兼容性

| 测试项 | 测试类型 | 覆盖场景 | 预期结果 |
|--------|----------|----------|----------|
| 搜狗拼音输入法注入 | 手动 (集成) | 搜狗拼音开启状态下执行语音输入 | 文字进入文本框，不进入候选框 |
| 百度输入法注入 | 手动 (集成) | 百度输入法开启状态下执行语音输入 | 文字进入文本框，不进入候选框 |
| 系统拼音输入法注入 | 手动 (集成) | 系统拼音开启状态下执行语音输入 | 文字进入文本框，不进入候选框 |
| 繁体注音输入法注入 | 手动 (集成) | 繁体注音开启状态下执行语音输入 | 文字进入文本框，不进入候选框 |
| 输入法切换后恢复 | 单元 | 切换到 ASCII → 粘贴 → 恢复原输入法 | 原输入法恢复 |
| 剪贴板内容保护 | 单元 | 用户剪贴板有内容 → 语音输入 → 注入 | 注入后剪贴板内容不变 |

### 1.3 NSPanel 浮窗特性

| 测试项 | 测试类型 | 覆盖场景 | 预期结果 |
|--------|----------|----------|----------|
| 浮窗不抢夺焦点 | 手动 | 录音浮窗显示时点击其他应用 | 其他应用保持焦点 |
| 多显示器环境 | 手动 | 连接外接显示器，在不同屏幕触发 | 浮窗出现在鼠标所在屏幕 |
| Retina 显示器清晰度 | 手动 | 在 Retina 屏幕上查看浮窗 | 波形清晰，无锯齿 |
| ProMotion 120Hz 适配 | 手动 | 在 120Hz 设备上查看波形动画 | 动画流畅 (CADisplayLink 同步) |
| 浮窗淡入淡出 | 手动 | Fn 按下/释放时观察浮窗 | 淡入 300ms，淡出 300ms |
| 浮窗层级正确 | 手动 | 录音时打开其他浮窗式应用 | 浮窗始终在最前 (level: .floating) |

### 1.4 LSUIElement 菜单栏应用

| 测试项 | 测试类型 | 覆盖场景 | 预期结果 |
|--------|----------|----------|----------|
| 无 Dock 图标 | 手动 | 查看 macOS Dock | 应用图标不在 Dock 中 |
| 菜单栏图标存在 | 手动 | 查看菜单栏 | mic.fill 图标显示 |
| 菜单栏图标高对比度适配 | 手动 | 开启高对比度模式 | 图标颜色正确适配 |
| 右键上下文菜单 | 手动 | 右键点击菜单栏图标 | 显示"设置"和"退出" |
| 左键打开设置面板 | 手动 | 左键点击菜单栏图标 | Popover 设置面板弹出 |

### 1.5 菜单栏设置面板

| 测试项 | 测试类型 | 覆盖场景 | 预期结果 |
|--------|----------|----------|----------|
| 语言切换生效 | 集成 | 选择英文 → 语音输入 | 英文语音被正确识别 |
| 快捷键配置 | 集成 | 自定义快捷键 → 触发录音 | 自定义键生效 |
| 开机启动开关 | 手动 | 开启开机启动 → 重启 Mac | 应用自动启动 |
| 设置面板打开时间 | 性能 | 从点击到面板可见 | < 500ms |

### 1.6 公证 (Notarization) 兼容性

| 测试项 | 测试类型 | 覆盖场景 | 预期结果 |
|--------|----------|----------|----------|
| 公证后应用运行 | 手动 | 下载公证后的 .app | Gatekeeper 不拦截 |
| Hardened Runtime 兼容 | 手动 | 在严格模式下运行 | 功能正常运行 |
| Accessibility 在公证后可用 | 手动 | 公证后首次授权 | 权限正常授予 |

---

## 2. iOS 平台特性测试

### 2.1 后台定位

| 测试项 | 测试类型 | 覆盖场景 | 预期结果 |
|--------|----------|----------|----------|
| 后台定位持续 | 手动 | 应用切换到后台，开始导航 | 语音播报持续 |
| 后台定位精度 | 手动 | 户外开阔地带后台导航 | 水平误差 < 5m |
| 后台定位电量 | 手动 | 后台导航 30 分钟 | 电量消耗合理 (参考性能基准) |
| 后台定位被系统终止后恢复 | 手动 | 后台被终止 → 重新启动 | 恢复上次导航状态 |
| 定位权限 Always vs WhenInUse | 手动 | 仅授权 WhenInUse → 后台导航 | 提示升级到 Always 权限 |

### 2.2 APNs 推送通知

| 测试项 | 测试类型 | 覆盖场景 | 预期结果 |
|--------|----------|----------|----------|
| 推送通知到达 | 集成 | 发送测试推送 | < 5s 到达设备 |
| 通知点击响应 | 手动 | 点击通知 | < 100ms 打开应用并进入详情 |
| 到达提醒语音播报 | 手动 | 推送到达后 | 自定义语音播报，不依赖静音 |
| 离线推送 | 手动 | 设备离线时发送推送 | 联网后立即收到 |
| 推送权限拒绝降级 | 手动 | 拒绝推送权限 | 使用 in-app banner 替代 |
| 后台远程通知触发 | 手动 | 应用在后台时收到位置相关推送 | 触发后台处理 |

### 2.3 离线地图

| 测试项 | 测试类型 | 覆盖场景 | 预期结果 |
|--------|----------|----------|----------|
| 城市离线包下载 | 手动 | 下载北京市离线地图 | < 50MB 包体积 |
| 离线模式切换 | 手动 | 断开网络 → 查看导航 | 自动切换到离线模式 |
| 离线导航可用性 | 手动 | 离线模式下开始导航 | 语音播报 + 位置跟踪可用 |
| 存储空间不足禁止下载 | 手动 | 可用空间 < 500MB → 下载 | 提示清理空间 |
| 离线数据过期提示 | 手动 | 离线数据 > 30 天未更新 | 提示用户更新 |
| WiFi 下自动更新 | 手动 | 连接 WiFi 且离线数据过期 | 自动检查并提示更新 |

### 2.4 VoiceOver 无障碍

| 测试项 | 测试类型 | 覆盖场景 | 预期结果 |
|--------|----------|----------|----------|
| 所有元素可朗读 | 手动 | 开启 VoiceOver 浏览全部 UI | 0 个不可朗读元素 |
| 地图 VoiceOver 支持 | 手动 | VoiceOver 划过地图区域 | POI 和路线信息被朗读 |
| 自定义 VoiceOver 手势 | 手动 | 双击地图中心 | 放大当前路段信息 |
| 焦点跟随语音 | 手动 | 语音播报时 | 焦点自动移动到相关 UI |
| Dynamic Type 200% | 手动 | 设置系统字体到最大 | 无文本溢出 |
| 高对比度支持 | 手动 | 开启高对比度模式 | 配色正确，边界清晰 |
| 减少动画 | 手动 | 开启减少动画选项 | 语音播报无多余动画 |

### 2.5 CoreMotion 步态检测

| 测试项 | 测试类型 | 覆盖场景 | 预期结果 |
|--------|----------|----------|----------|
| CMPedometer 可用性 | 单元 | iOS 8 以下设备 | 优雅降级，不崩溃 |
| 室内定位辅助 | 手动 | 室内 WiFi 定位 | 精度提升 |
| 运动权限请求 | 手动 | 首次启动请求运动权限 | 清晰说明用途 |

### 2.6 AVSpeechSynthesizer 语音播报

| 测试项 | 测试类型 | 覆盖场景 | 预期结果 |
|--------|----------|----------|----------|
| 语音合成延迟 | 性能 | 位置更新触发到语音开始 | < 200ms |
| 语速配置 | 手动 | 设置 voiceRate = 0.5 | 播报语速适中，适合视障用户 |
| 播报优先级 | 手动 | 播报时后台播放音乐 | 语音高于背景音乐 |
| 语音合成失败降级 | 单元 | 模拟合成失败 | 回退到系统默认 TTS |
| 用户打断播报 | 手动 | 播报过程中有新位置更新 | 立即停止当前播报 |
| 静音模式绕过 | 手动 | 设备静音时导航通知 | 使用自定义语音通道 |

---

## 3. Android 平台特性测试

### 3.1 后台 Service

| 测试项 | 测试类型 | 覆盖场景 | 预期结果 |
|--------|----------|----------|----------|
| 后台 Service 保活 | 手动 | 应用切换到后台 | 导航 Service 持续运行 |
| 系统省电策略兼容 | 手动 | 在不同厂商省电模式下 | 不被强制休眠 |
| Foreground Service 通知 | 手动 | 后台导航时 | 显示持续运行通知 |
| Doze 模式兼容 | 手动 | 设备进入 Doze | 导航不被中断 |
| 后台定位 (FusedLocationProvider) | 手动 | 后台导航 | 位置更新持续 |

### 3.2 跨应用文本注入

| 测试项 | 测试类型 | 覆盖场景 | 预期结果 |
|--------|----------|----------|----------|
| AccessibilityService 授权 | 手动 | 首次启动授权 | 引导到系统设置 |
| 注入到不同应用 | 手动 | 注入到微信/钉钉/浏览器等 | 文字正确出现在焦点位置 |
| 覆盖粘贴板保护 | 手动 | 用户粘贴板有内容时注入 | 注入后恢复原内容 |
| 输入法兼容性 | 手动 | 在各中文输入法下注入 | 与 CJK 兼容 |

---

## 4. Web 平台特性测试

### 4.1 Web Speech API

| 测试项 | 测试类型 | 覆盖场景 | 预期结果 |
|--------|----------|----------|----------|
| SpeechRecognition 在线识别 | E2E | 说中文 | 正确转为文字 |
| SpeechRecognition 离线降级 | E2E | 断开网络后识别 | 提示离线不可用或使用离线引擎 |
| SpeechSynthesis 播报 | E2E | 触发语音播报 | 正确朗读内容 |
| 麦克风权限请求 | E2E | 首次使用 | 清晰说明用途的权限提示 |
| 浏览器兼容 (Chrome/Safari/Firefox) | E2E | 在各浏览器中测试 | 功能在各浏览器中可用 |

### 4.2 Clipboard API

| 测试项 | 测试类型 | 覆盖场景 | 预期结果 |
|--------|----------|----------|----------|
| 写入剪贴板 | 单元 + E2E | 将识别结果写入剪贴板 | 粘贴后内容正确 |
| 读取剪贴板 | E2E | 读取用户剪贴板 | 正确读取 |
| HTTPS 要求 | E2E | 在 HTTP 页面中使用 | 权限被拒绝或降级 |

### 4.3 Service Worker

| 测试项 | 测试类型 | 覆盖场景 | 预期结果 |
|--------|----------|----------|----------|
| 离线缓存 | E2E | 断网后刷新页面 | 显示离线缓存内容 |
| 后台同步 | E2E | 离线时触发操作 → 联网 | 操作自动同步 |
| 推送通知 | E2E | 订阅推送 → 触发推送 | 收到通知 |

### 4.4 PWA

| 测试项 | 测试类型 | 覆盖场景 | 预期结果 |
|--------|----------|----------|----------|
| 安装到主屏幕 | E2E | 点击安装 → 查看主屏幕 | 图标存在且可打开 |
| manifest.json 完整性 | 单元 | 检查 manifest 字段 | 包含 name, icons, start_url 等 |
| HTTPS 要求 | E2E | 在 HTTP 下运行 | PWA 功能受限或不可用 |

---

## 5. 跨平台兼容性测试

| 测试项 | 测试场景 | macOS | iOS | Android | Web |
|--------|----------|-------|-----|---------|-----|
| 中文语音识别准确率 | 普通话 50 句测试 | P/F | P/F | P/F | P/F |
| 英文语音识别准确率 | 英语 50 句测试 | P/F | P/F | P/F | P/F |
| 深色模式支持 | 系统切换到深色模式 | P/F | P/F | P/F | P/F |
| 低电量模式兼容 | 系统进入低电量模式 | N/A | P/F | P/F | N/A |
| 网络切换 (WiFi↔Cellular) | 导航中切换网络 | N/A | P/F | P/F | P/F |
| 屏幕旋转 | iPad 横竖屏切换 | N/A | P/F | P/F | P/F |
