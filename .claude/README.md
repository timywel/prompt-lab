# Skills Location

Skills have been moved to the root `skills/` directory.

To install Claude Code project-level skills:

```bash
make install-claude
# or
./adapters/claude-code/install.sh
```

This creates a symlink: `.claude/skills` → `skills/`

---

All 8 PRD Generator skills are available in the root `skills/` directory.
